#! /bin/csh -f

# restart gatekeeper and put log in pipeline directory.

set WORKFLOW_DATA = /home/jsoc/pipeline
set WORKFLOW_ROOT = /home/phil/jsoc/proj/workflow

cd $WORKFLOW_DATA
rm -f Keep_running

while (-e GATEKEEPER_BUSY)
  sleep 1
end

$WORKFLOW_ROOT/gatekeeper.csh >& log &

